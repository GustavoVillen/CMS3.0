# Web Legacy Adaptation App

Purpose: temporary host for the migrated frontend while the new backend is built.

This is not the old Google Apps Script frontend.

This app should receive:
- the compatibility layer that replaces `google.script.run`
- routing and bootstrap wiring
- i18n/session-aware rendering
